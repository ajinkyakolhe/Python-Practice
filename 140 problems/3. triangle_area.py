# You need base and height as input for calculating the area of a trianlge
# Formula is 1/2 * b * h

base = float(input(" Enter the length of the base: "))
height = float(input(" Enter the length of the height: "))
area = 1/2 * base * height

# Display the area
print(f"The area of the triangle: {area}")